# ===============================
# 모듈: 모방학습.md
# 설명: 모방학습 개요 정리
# ===============================

class 모방학습:

    def __init__(self):
        self.정의 = "전문가의 행동을 모방하여 학습하는 머신러닝 기법"
        self.보상함수필요여부 = False

    def 정의_설명(self):
        """
        모방학습이란 무엇인가?
        """
        return """
        전문가(Expert)의 상태(State)와 행동(Action) 데이터를 학습하여
        동일하거나 유사한 행동을 수행하도록 하는 학습 방법이다.
        """

    def 필요성(self):
        """
        왜 사용하는가?
        """
        return [
            "보상 함수 설계가 어려운 문제 해결 가능",
            "사람의 직관과 숙련된 기술을 학습 가능",
            "빠르고 안정적인 학습 가능"
        ]

    def 주요방법(self):
        """
        주요 학습 방법
        """
        return {
            "행동 복제": {
                "유형": "지도학습",
                "입력": "상태(State)",
                "출력": "행동(Action)",
                "장점": "구현이 간단함",
                "단점": "오류 누적 가능"
            },
            "역강화학습": {
                "유형": "보상 함수 추정",
                "장점": "일반화 성능 우수",
                "단점": "계산 비용 큼"
            },
            "DAgger": {
                "유형": "데이터셋 확장",
                "장점": "오류 누적 문제 해결",
                "단점": "전문가 개입 필요"
            }
        }

    def 장점(self):
        """
        장점
        """
        return [
            "보상 함수 설계 불필요",
            "구현이 비교적 쉬움",
            "실제 인간 행동 반영 가능"
        ]

    def 단점(self):
        """
        단점
        """
        return [
            "전문가 데이터 의존성 높음",
            "새로운 상황 대응 어려움",
            "오류 누적 가능성 존재"
        ]

    def 활용분야(self):
        """
        활용 분야
        """
        return [
            "자율주행 자동차",
            "로봇 제어",
            "게임 AI",
            "의료 시스템"
        ]

    def 강화학습과비교(self):
        """
        강화학습과 비교
        """
        return {
            "모방학습": {
                "데이터": "전문가 데이터",
                "보상함수": "불필요",
                "학습속도": "빠름"
            },
            "강화학습": {
                "데이터": "환경과의 상호작용",
                "보상함수": "필요",
                "학습속도": "느릴 수 있음"
            }
        }


# ===============================
# 실행 예시
# ===============================

if __name__ == "__main__":
    모델 = 모방학습()

    print(모델.정의_설명())
    print(모델.필요성())
    print(모델.주요방법())
